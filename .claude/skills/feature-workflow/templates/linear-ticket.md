## Context

Why this work exists now. What in the current implementation or design doc
motivates it. Link the relevant `Docs/Technical Design Docs/*.md` section.

## Scope

What this ticket changes, as a short list of concrete outcomes.

- [ ] ...
- [ ] ...

## Out of scope

What a reader might reasonably assume is included but is not, and which ticket
covers it instead (or "not yet ticketed").

## Invariants and design contracts

The properties the change must preserve or establish — durability, ordering,
locking discipline, crash-recovery behavior, on-disk format compatibility.
State them precisely; this is the part reviewers check against.

## Acceptance criteria

Observable conditions that make this ticket done. Written so someone else can
verify them without reading the diff.

## Validation plan

Which tests prove it: new unit tests, existing suites that must stay green,
crash/recovery scenarios to exercise manually.

## References

Design docs, prior tickets, PRs, book sections.
